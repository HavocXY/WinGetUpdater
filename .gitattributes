* text=auto
*.png binary

# Testdaten sind unveränderte winget-Ausgaben. Zeilenenden bleiben unangetastet,
# damit die Dateien wirklich das sind, was winget geschrieben hat.
tests/WinGetStudio.Tests/Fixtures/*.txt -text
